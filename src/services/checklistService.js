// require('./config/database')

// const ChecklistModel = require('./src/models/checklistModel')

// let checklist = new ChecklistModel({})

// // ChecklistModel.save().then(res => console.log(res).catch(e => console.log(e)))

// checklist.save().then(res => console.log(res)).catch(e => console.log(e))

// ChecklistModel.create({name: 'Tarefa123'}, (err, checklist) => console.log(err))

// // checklist.name = 'Tarefa123'